// Auto-generated by pa app init + pa app add data-source
// Do NOT edit .power/schemas/appschemas/dataSourcesInfo.ts manually
import { dataSourcesInfo as generated } from "../../.power/schemas/appschemas/dataSourcesInfo"

// System tables not included in generated metadata
const systemTables = {
  systemusers: {
    tableId: "systemuser",
    version: "",
    primaryKey: "systemuserid",
    dataSourceType: "Dataverse",
    apis: {},
  },
}

// Flow connector — only present when VITE_FEATURE_APPROVAL_FLOW=true is configured
// Added via: npx pa app add flow --flow-id {FLOW_WORKFLOW_ID}
const flowConnectors = {} // populated after pa app add flow for the approval flow

export const dataSourcesInfo = {
  ...generated,
  ...systemTables,
  ...flowConnectors,
}
