# code-apps — 環境変数サンプル
# このファイルはプレースホルダーのみ。実値はリポジトリルートの .env に置く（.gitignore 済み）。
# 値は scripts/ の各スクリプトが参照する（add_app_to_solution.py / toggle_table_lang.py / pre-deploy-check.mjs）。

# === Power Platform 環境（必須）===
# 取得元: Power Platform 管理センター > 環境 > 環境 URL / ID
DATAVERSE_URL=https://{org}.crm.dynamics.com/
TENANT_ID={your-tenant-id}
ENV_ID={your-environment-id}

# === ソリューション設定（必須・テーマに合わせて変更）===
# 取得元: pac solution list または メーカーポータル > ソリューション
SOLUTION_NAME={YourSolutionName}
# 取得元: メーカーポータル > 発行元 のカスタマイズプレフィックス
PUBLISHER_PREFIX={prefix}

# === アプリ ID（add_app_to_solution.py 用）===
# 取得元: power.config.json の appId、または pac code push 後に払い出される Code App の ID
APP_ID={app-id}
