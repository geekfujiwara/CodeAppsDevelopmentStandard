{
  "name": "drawing-communication-addon",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22.18.0" },
  "scripts": {
    "test": "node --test \"tests/*.test.mjs\"",
    "typecheck": "tsc --noEmit",
    "test:skill": "python -X utf8 -m unittest discover -s tests -p test_renderer.py"
  },
  "devDependencies": {
    "typescript": "~5.9.3"
  }
}
