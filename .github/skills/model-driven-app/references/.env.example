# model-driven-app — 環境変数サンプル
# このファイルはプレースホルダーのみ。実値はリポジトリルートの .env に置く（.gitignore 済み）。
# 値は scripts/（deploy_model_driven_app.py / customize_views_forms.py）が参照する。

# === Power Platform 環境（必須）===
# 取得元: Power Platform 管理センター > 環境 > 環境 URL
DATAVERSE_URL=https://{org}.crm.dynamics.com/

# === ソリューション設定（必須）===
# 取得元: pac solution list または メーカーポータル > ソリューション
SOLUTION_NAME={YourSolutionName}
# 取得元: メーカーポータル > 発行元 のカスタマイズプレフィックス
PUBLISHER_PREFIX={prefix}

# === モデル駆動型アプリ設定 ===
# アプリの一意名（英語のみ。ソリューション prefix が自動付与される）
APP_UNIQUE_NAME={prefix}_yourapp
# アプリの表示名（日本語可）
APP_DISPLAY_NAME={アプリ表示名}
# アプリの説明（任意）
APP_DESCRIPTION={アプリの説明}
